



function pro(name,setValue){
  let client = {};
//   function df(str) {
//     let tmp = /^[А-Яа-яЁё\s]+$/i.test(str);
//     return tmp;
//   }
//   if (df(name)) {
//     alert("true");
//   }
let fullName = name.match(/^[А-Яа-яЁё\s]+$/i)
if(fullName)
console.log(name)
name.match(/^[А-Яа-яЁё\s]+$/i);
alert(name.match(/^[А-Яа-яЁё\s]+$/i));

  // function func(str) {
  //    let tmp = /^[А-Яа-яЁё\s]+$/i.test(str);
  //    return tmp
  // }
  // console.log(func(value.value.trim()));

  //    console.log(df(value.value.trim()));
  return client;
}

export default pro